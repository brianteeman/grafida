<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the signed, notarised Grafida.app.

  Grafida's main binary is a Boson self-extracting (micro.sfx) executable that
  bundles a PHP runtime and loads a separate .dylib. Under the hardened runtime
  (required for notarisation) these need a few exceptions:

    * disable-library-validation      — load the bundled dylib, which is not
                                         signed by Apple / a different Team than
                                         the loading process expects.
    * allow-jit / allow-unsigned-
      executable-memory               — the self-extracting runtime maps and
                                         executes code (and PHP opcache JIT, if
                                         enabled) from writable memory.
    * allow-dyld-environment-variables — Boson sets dyld env vars to locate its
                                         bundled runtime/dylib at launch.

  Trim this to the minimum that still launches + notarises: start with all four,
  confirm the signed build runs and passes notarisation, then remove them one at
  a time. Fewer entitlements is always better.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>